# ==================== GitHub Pages 部署策略 ====================
# 只提交网页运行必需的文件，其余辅助/开发文件保留在本地

# 先忽略所有文件（反向白名单模式）
/*

# 只允许网页部署必需的文件
!/index.html
!/logo.png
!/.gitignore

# 下面这些文件可以保留在本地但不应提交
# AI_PROMPT.md   - 开发助手提示词
# analyze_pdf.py - Python 辅助解析脚本
# package.json   - 仅记录依赖（前端不用 npm）
